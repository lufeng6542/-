# 视频创作工具

AI视频生成 + AI图片生成 + 视频剪辑一站式工具

## 项目结构

```
D:/视频剪辑项目/
├── ai_generator/            # AI生成模块
│   ├── zhipu_client.py      # 智谱API客户端（图片+视频+视觉分析）
│   ├── image_generator.py   # AI图片生成（CogView-4）
│   ├── video_generator.py   # AI视频生成（CogVideoX-3，文生/图生视频）
│   └── quality_checker.py   # 视频质量验证
├── core/                    # 核心剪辑模块
│   ├── video_splitter.py    # 视频分割
│   ├── video_editor.py      # 视频编辑合成
│   ├── vocal_separator.py   # 人声分离
│   └── explosion_analyzer.py # 爆点分析
├── scripts/                 # 剪辑脚本
│   ├── auto_editor.py       # 自动剪辑流程
│   └── archive/             # 归档的一次性脚本
├── cli.py                   # 统一命令行入口
├── 素材/                    # 视频素材
├── 输出/                    # 输出视频
├── BGM/                     # 背景音乐
└── models/                  # AI模型
```

## 命令行使用

### 剪辑命令

```bash
python cli.py split -i 视频.mp4 -d 4          # 分割视频（每段4秒）
python cli.py separate -i 视频.mp4            # 人声分离
python cli.py edit -i 片段目录 -b BGM.mp3     # 视频编辑合成
python cli.py analyze -i 视频.mp4             # 爆点分析
python cli.py auto                            # 自动剪辑流程
```

### AI生成命令

```bash
python cli.py gen image "一只猫" --ar 16:9                    # AI生成图片
python cli.py gen video "海浪拍打沙滩" --ar 16:9              # 文生视频
python cli.py gen video "海浪" --auto-verify                  # 生成后自动验证
python cli.py gen img2video 首帧.png "猫咪奔跑" --ar 9:16     # 图生视频
python cli.py verify -i 视频.mp4 "海浪拍打沙滩"               # 视频质量验证
python cli.py task-status <任务ID>                             # 查询任务状态
```

## API Key 配置

编辑 `~/.baoyu-skills/.env`：
```
ZHIPU_API_KEY=你的密钥
```

## 项目记录

### 2026-03-27
- 新增 ai_generator 模块（AI图片/视频生成 + 质量验证）
- CLI 新增 gen / verify / task-status 命令
- CLI 增强：进度条、Ctrl+C安全中断、时间预估、--auto-verify
- scripts 精简：17个一次性脚本归档到 scripts/archive/
- 素材清理：删除重复文件，释放 2.7GB

### 2026-03-03
- 爆款片段分析器（Whisper转写 + 爆点识别）
- MDX23C人声分离模型

### 2026-03-01
- 从B站下载海贼王素材，分割/合成/换BGM/转竖屏
